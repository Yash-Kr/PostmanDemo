# This is a basic workflow to help you get started with Actions

name: Automated API tests using newman CLI

# Controls when the workflow will run
on: workflow_dispatch

#   # Triggers the workflow on push or pull request events but only for the "main" branch
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

#   # Allows you to run this workflow manually from the Actions tab
#   workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "test-api"
  test-api:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Install node on the runner
      - name: Cache npm modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
            
      - name: Install Node
        uses: actions/setup-node@v3
      - name: Install newman
        run: |
          npm install -g newman
      - name: Install newman-reporter-html
        run: |
          npm install -g newman-reporter-htmlextra 
      - name: Checkout the main branch
        uses: actions/checkout@v2
      - name: Run Postman collection
        run: |
          newman run "myCollection.json" -r htmlextra  --reporter-htmlextra-export output.html
          echo $(cat output.html)" >> index.html
      - name: Commit and push the changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Update index.html
          push: true
  
#   update-file:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout the main branch
#       uses: actions/checkout@v2
#     - name: Run Postman collection
#       run: |
#         newman run GoRestApi.postman_collection.json -r html --reporter-html-template mytemplate.hbs > index.html
#     - name: Commit and push the changes
#       uses: stefanzweifel/git-auto-commit-action@v4
#       with:
#         commit_message: Update index.html
#         push: true ,,, 
        
        
  
